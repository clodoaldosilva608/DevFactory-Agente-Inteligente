// Build assets — placeholder icons
//
// For production, replace these with actual icon files:
// - build/icon.ico (Windows, multi-resolution 256x256, 128x128, 64x64, 32x32, 16x16)
// - build/icon.icns (macOS, with retina variants)
// - build/icon.png (Linux, 512x512)
//
// Use https://www.electron.build/icons to generate from a single PNG.
//
// For now, the build will work without icons (electron-builder falls back to default).
